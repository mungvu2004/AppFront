T5 · hook — LỚP 2, viết mã: toàn bộ logic màn /billing

[BỐI CẢNH — HỢP ĐỒNG VÀ NOTES]
Bạn là worker trong DAG 7 task dựng màn /billing của repo AppFront. Điều phối viên đang giám sát.
Ba lớp 1 đã xong và kết quả của họ nằm trên nhánh bạn đang đứng. Đọc BẮT BUỘC, theo thứ tự:
  1. docs/contracts/billing/CONTRACT.md — hợp đồng đông cứng đã duyệt. Mục 2 là KIỂU BẠN PHẢI KHAI
     (BillingScreenViewModel = y hệt BillingScreenProps, từng trường, đúng tên). Mục 3.1 là hàm
     formatMoney bạn phải khai và xuất. Mục 4 là bảy trạng thái. Mục 7 là chuyển động.
  2. docs/contracts/billing/format-motion.md (T1) — chữ ký thật của format và useCountUp.
  3. docs/contracts/billing/data.md (T2) — query, mutation, lỗi L-03, quyền, phán quyết P-07.
  4. src/screens/billing/BillingScreen/billingGateway.ts (T4) — nguồn dữ liệu của bạn.
  5. src/screens/onboarding/WelcomeScreen/useWelcomeScreen.ts — khuôn hook màn gần nhất.
  6. LUAT_MAN_HINH.md R-61, R-64, R-69, R-71.
KHÔNG viết view. KHÔNG sửa file của người khác. Hợp đồng hoặc notes sai/thiếu →
`orca orchestration ask --question "..." --timeout-ms 600000 --json` rồi DỪNG chờ trả lời.

[VIỆC CỦA BẠN]
Viết src/screens/billing/BillingScreen/useBillingScreen.ts. Xuất:
  useBillingScreen(options), BillingScreenViewModel, UseBillingScreenOptions, formatMoney,
  và mọi kiểu ở hợp đồng mục 2 (BillingPeriod, QuotaTone, InvoiceStatus, BillingBlock,
  BillingScreenState, BillingLabelledValue, BillingCurrentPlan, BillingPlanCard, BillingEstimate,
  BillingInvoiceRow, BillingInvoicePage, BillingConfirmSummary, BillingDegradedNotice,
  BillingErrorNotice). BillingScreenViewModel có ĐÚNG các trường của BillingScreenProps.

Ràng buộc, mỗi cái là một điểm nghiệm thu:
1. TRẠNG THÁI MÁY CHỦ QUA @tanstack/react-query (R-64). CẤM `useState` cho loading/error —
   `rg "useState.*([Ll]oading|error)"` trên file bạn phải RỖNG. Khoá query khai theo khuôn T2 ghi lại.
2. BẢY TRẠNG THÁI suy ra một chỗ duy nhất, trả về qua trường `state` (hợp đồng mục 4).
   Không nhánh hiển thị rời rạc.
3. MỌI *Label LÀ CHUỖI ĐÃ ĐỊNH DẠNG. View không bao giờ thấy một con số cần định dạng.
   Diện tích qua formatArea, tiền qua formatMoney (hợp đồng mục 3.1), ngày/kỳ qua src/lib/format/datetime.
   CẤM toFixed, toLocaleString, và mọi phép chia quy đổi đơn vị (local/no-raw-number bắt cả src/screens).
4. TẠM TÍNH ĐỌC TỪ DỮ LIỆU (estimate.subtotalVnd), usedRatio đọc từ dữ liệu. KHÔNG nhân chia trong màn.
5. NGƯỠNG 80%: vượt thì tone = 'attention' VÀ quotaAlert hiện với câu
   "Sắp hết hạn mức. Còn {còn lại}." (còn lại qua formatArea fractionDigits 0). Màu/thang lấy theo
   phán quyết P-07 mà T2 đã chốt trong data.md — KHÔNG tự chọn màu. Ngưỡng 0.8 là hằng có tên.
6. ĐỔI KỲ THANH TOÁN: priceLabel CHẠY SỐ sang giá mới bằng useCountUp (260ms, hợp đồng Q4/mục 7),
   không thay chữ đột ngột. Đây là phần khó nhất — đọc kỹ chữ ký retarget trong format-motion.md.
7. QUYỀN (trạng thái 6): vai Kỹ sư và Người xem → isReadOnly = true, readOnlyNotice =
   "Chỉ quản trị viên có thể thay đổi gói.", canChangePlan = false, mọi nút đổi/nâng gói vô hiệu.
   Biểu thức quyền lấy đúng như T2 ghi trong data.md.
8. NÂNG GÓI: gọi quoteChangePlan → trả về `confirm` (bảng tóm tắt xác nhận có số tiền chia theo tỷ lệ)
   → chỉ khi onConfirmAccept mới gọi confirmChangePlan. Đây là A9 (hành động không hoàn tác được thì
   phải hỏi trước). Nếu T2 kết luận nên đi qua createOptimisticMutation thì theo kết luận đó.
9. LỖI CỔNG THANH TOÁN dịch qua L-03 (toAppError + describeError). Trường `code` lấy đúng nguồn mà
   T2 chỉ ra. CẤM tự chế mã lỗi.
10. TRẠNG THÁI MỘT PHẦN: quota đang tính lại HOẶC hoá đơn không lấy được → `degraded` chỉ chứa
    khối bị ảnh hưởng, phần còn lại vẫn đầy đủ dữ liệu.
11. Phân trang 10 dòng/trang qua invoicePage.
12. Không hằng số ma thuật: 0.8, 10, 260, 340 đều phải là hằng có tên hoặc lấy từ src/lib (R-71).

Chuỗi tiếng Việt viết thẳng vào TS, lấy đúng bảng ở hợp đồng mục 5. Khoá mới → ghi vào
docs/contracts/billing/i18n/t5.fragment.json. KHÔNG tự sửa src/i18n/vi.json.

[CẤM TUYỆT ĐỐI — nguyên văn từ prompt gốc]
- Không thẻ gói nào có nền màu, không băng nhiều màu, không "phổ biến nhất" nổi bật.
- Dấu tích tính năng màu trung tính, không xanh lá.
- Không đỏ đặc cho hoá đơn quá hạn; dùng badge nền nhạt.
- Không tự định dạng số, không tự tính tạm tính.
- Không tạo component mới.
[KHÔNG ĐƯỢC SỬA FILE NÀO]
- src/lib/**, src/api/**, src/domain/**, src/store/**, src/components/**, AGENTS.md, các màn đã xong.
Thêm: không TODO/FIXME/stub, không `any`, không sửa/nới/tắt bài kiểm có sẵn (R-70), không chuỗi bắt
đầu bằng '/' hay 'http' (R-65), không sửa CONTRACT.md, không sửa billingGateway.ts của T4.

[FILE ĐƯỢC SỬA — ĐÚNG HAI]
- src/screens/billing/BillingScreen/useBillingScreen.ts
- docs/contracts/billing/i18n/t5.fragment.json
Không file nào khác.

[NGHIỆM THU — PHẢI IN SỐ VÀO worker_done]
  pnpm typecheck        → 0 lỗi
  pnpm lint             → 0 cảnh báo, 0 lỗi
  pnpm test             → "N passed | M failed", không làm đỏ bài kiểm nào đang xanh
  pnpm length           → số dòng file của bạn (trần cứng 400)
  rg -n "useState.*([Ll]oading|error)" src/screens/billing   → phải RỖNG, dán kết quả
  rg -n "toFixed|toLocaleString" src/screens/billing         → phải RỖNG, dán kết quả
  git diff --name-only  → đúng 2 dòng
Trong --body ghi thêm: 7 trạng thái đã ánh xạ (liệt kê), ngưỡng dùng hằng tên gì, hàm chạy số dùng
chữ ký nào, biểu thức quyền dùng hàm nào. Thiếu số = chưa xong. Commit trước khi gửi worker_done.

---

## PHÁN QUYẾT BỔ SUNG CỦA ĐIỀU PHỐI VIÊN — đọc trước khi viết, ràng buộc như hợp đồng

Ba điểm dưới đây do lớp 1 phát hiện và điều phối viên đã phê chuẩn. Không tự quyết lại.

1. **P-07 / Q8 — CHỐT.** Dùng `createLookupScale<QuotaTone>` của `src/lib/coloring/scales.ts:338-349`,
   bảng `{ normal: '--accent', attention: '--state-attention-tint' }`. **Không** dùng
   `createQuantileScale` cũng **không** dùng `SEQUENTIAL_RAMP`: thanh hạn mức có hai trường hợp cố
   định, không phải một dải số cần cắt phân vị. Chi tiết và trích dẫn dòng ở `data.md` mục 5.
   Màu **không** được chọn tay ở hook hay view — đi qua hàm này.

2. **Quyền — CHỐT.** `permissionMatrix` **không có** `PermissionKey` nào cho billing, và `src/lib/**`
   là thư mục cấm sửa (R-68). Dùng `roles` của `SessionSnapshot`: quản trị là
   `session.roles.includes('admin')`. **Không** thêm khoá vào ma trận quyền, **không** tự chế hàm
   `can` thứ hai. Ghi một dòng JSDoc nói đây là nợ, mã đề xuất **T-10** (thêm
   `PermissionKey 'billing.manage'` vào `src/lib/auth/permissions.ts` ở một lượt logic riêng).

3. **`useCountUp` trả `text`, không trả `value`.** Xem `format-motion.md` mục 5. Chuỗi giá hiển thị
   lấy thẳng từ `text`; đừng tự định dạng lại kết quả.

**Môi trường:** `pnpm` trong worktree này đã sửa xong, chạy được `pnpm typecheck`, `pnpm lint`,
`pnpm test`, `pnpm length`. Nếu thấy `pnpm-workspace.yaml` hiện ra trong `git status` thì đó là
pnpm ghi đè, **không phải việc của bạn** — chạy `git checkout -- pnpm-workspace.yaml` và **tuyệt đối
không** commit nó. Chỉ commit đúng hai file trong phạm vi của bạn.
