T4 · foundation — LỚP 1, viết mã: nguồn dữ liệu mock của màn /billing

[BỐI CẢNH — HỢP ĐỒNG VÀ NOTES]
Bạn là worker trong DAG 7 task dựng màn /billing của repo AppFront. Điều phối viên đang giám sát.
Đọc BẮT BUỘC trước khi viết dòng đầu tiên:
  1. docs/contracts/billing/CONTRACT.md — hợp đồng đông cứng đã duyệt. Mục 2.1 là KIỂU BẠN PHẢI
     KHAI, từng trường, đúng tên. T5 chép từ đó. Đổi một tên trường = T5 gãy.
  2. src/screens/account/AccountSettings/accountSettingsGateway.ts — JSDoc dòng 1-32 giải thích
     CHÍNH XÁC hoàn cảnh của bạn: không có endpoint, src/api/** cấm sửa, nên dữ liệu ở trong module.
     Đây là khuôn bạn chép. Đọc thêm src/screens/project/ProjectSettings/projectSettingsGateway.ts.
  3. LUAT_MAN_HINH.md R-61 (hook chỉ nối logic đã có), R-69 (thiếu logic thì DỪNG và hỏi).
Hợp đồng sai → `orca orchestration ask --question "..." --timeout-ms 600000 --json`, DỪNG chờ trả lời.

[VIỆC CỦA BẠN]
Viết src/screens/billing/BillingScreen/billingGateway.ts — cổng dữ liệu duy nhất của màn.

LỆNH RIÊNG CỦA NGƯỜI DUYỆT: "nếu cần data, hãy thực hiện mock data mà KHÔNG hard code."
Nghĩa cụ thể, và đây là tiêu chí nghiệm thu:
  - Mọi con số sống trong ĐÚNG MỘT bảng dữ liệu có kiểu, khai một lần ở đầu module, đặt tên có
    nghĩa. Không một số nào rải trong thân hàm.
  - Danh sách hoá đơn phải SINH ra từ bảng đó bằng một hàm thuần tất định (cùng đầu vào → cùng đầu
    ra, không Math.random, không Date.now trong dữ liệu), đủ để phân trang 10 dòng/trang chứng minh
    được — tối thiểu 24 hoá đơn. Không chép tay 24 object literal.
  - Ngày tháng là hằng epoch ms tính từ một mốc khai tường minh, KHÔNG gọi Date.now() ở tầng dữ liệu
    (bài kiểm phải tất định).
  - Tạm tính (subtotalVnd) và usedRatio TÍNH SẴN Ở ĐÂY. Màn tuyệt đối không nhân chia — đó là lý do
    hai trường này tồn tại.

Phải khai và xuất, đúng tên trong hợp đồng mục 2.1:
  BillingPeriod, QuotaTone, InvoiceStatus, BillingQuota, BillingPlanOffer, BillingInvoice,
  BillingEstimateData, BillingSnapshot, BillingChangePlanQuote, BillingGateway,
  createBillingGateway(), resetBillingStore() (cho test, sản phẩm không gọi).
Ba gói: đúng 3, mỗi gói ĐÚNG 6 tính năng, giá theo cả hai kỳ (monthly/yearly), một gói
isRecommended. Hạn mức mặc định: đã dùng 1842 m² / hạn mức 5000 m².
Cổng phải cho bài kiểm ép được các cảnh: hạn mức 85% (còn 620 m² → digitisedAreaM2 4380),
không có hoá đơn nào (trạng thái Rỗng), invoicesUnavailable = true và isRecalculating = true
(trạng thái Một phần), và một lối ném lỗi để trạng thái Lỗi dựng được. Cách ép cảnh là tham số/hàm
seed tường minh, KHÔNG phải sửa mã.
Mọi hàm trả Promise (hợp đồng mục 2.1 giải thích vì sao).
JSDoc đầu file bằng tiếng Việt theo khuôn accountSettingsGateway.ts: vì sao là bộ nhớ trong chứ
không phải lời gọi mạng, và ghi rõ NỢ T-09 (thêm nhóm billing vào ENDPOINTS, cho gateway gọi
src/api/client.ts, rồi xoá bộ nhớ này — khi ấy đây là file duy nhất phải sửa).

Đồng thời viết docs/contracts/billing/i18n/t4.fragment.json — mọi chuỗi tiếng Việt do BẠN sinh ra
(tên gói, tên tính năng, nhãn trạng thái hoá đơn nếu bạn đặt ở tầng dữ liệu), dạng
{"billing":{"<khoá>":"<chuỗi>"}}. T7 gộp vào src/i18n/vi.json. KHÔNG tự sửa vi.json.

KHÔNG sửa src/routes/paths.ts — ROUTE_PATTERNS.billing = '/billing' và ROUTES.billing đã có sẵn
(phán quyết Q1). Kiểm bằng `rg -n "billing" src/routes/paths.ts` rồi thôi.

[CẤM TUYỆT ĐỐI — nguyên văn từ prompt gốc]
- Không thẻ gói nào có nền màu, không băng nhiều màu, không "phổ biến nhất" nổi bật.
- Dấu tích tính năng màu trung tính, không xanh lá.
- Không đỏ đặc cho hoá đơn quá hạn; dùng badge nền nhạt.
- Không tự định dạng số, không tự tính tạm tính.
- Không tạo component mới.
[KHÔNG ĐƯỢC SỬA FILE NÀO]
- src/lib/**, src/api/**, src/domain/**, src/store/**, src/components/**, AGENTS.md, các màn đã xong.
Thêm, từ LUAT_MAN_HINH: không TODO/FIXME/stub, không `any`, không sửa/tắt bài kiểm có sẵn (R-70),
không chuỗi bắt đầu bằng '/' hay 'http' trong src/screens (R-65), không hằng số ma thuật không tên
(R-71), không sửa CONTRACT.md.

[FILE ĐƯỢC SỬA — ĐÚNG HAI]
- src/screens/billing/BillingScreen/billingGateway.ts
- docs/contracts/billing/i18n/t4.fragment.json
Không file nào khác. Đặc biệt KHÔNG src/routes/paths.ts, KHÔNG src/i18n/vi.json.

[NGHIỆM THU — PHẢI IN SỐ VÀO worker_done]
  pnpm typecheck        → 0 lỗi
  pnpm lint             → 0 cảnh báo, 0 lỗi (--max-warnings 0)
  pnpm test             → in "N passed | M failed" (không được làm đỏ bài kiểm nào đang xanh)
  pnpm length           → số dòng file của bạn
  git diff --name-only  → đúng 2 dòng
Trong --body ghi thêm: số hoá đơn sinh ra, số gói, số tính năng mỗi gói, usedRatio mặc định,
và một dòng chứng minh dữ liệu tất định (chạy hàm hai lần cho cùng kết quả).
Thiếu số = chưa xong. Commit trước khi gửi worker_done.
