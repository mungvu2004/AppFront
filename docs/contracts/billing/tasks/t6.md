T6 · view — LỚP 2, viết mã: view thuần màn /billing

[BỐI CẢNH — HỢP ĐỒNG VÀ NOTES]
Bạn là worker trong DAG 7 task dựng màn /billing của repo AppFront. Điều phối viên đang giám sát.
Đọc BẮT BUỘC, theo thứ tự:
  1. docs/contracts/billing/CONTRACT.md — hợp đồng đông cứng đã duyệt. Mục 2 là KIỂU PROPS BẠN PHẢI
     KHAI (BillingScreenProps, từng trường, đúng tên — T5 đang khai cùng kiểu đó song song với bạn;
     T7 sẽ gộp bằng import type. Đổi một tên = gãy). Mục 4 bảy trạng thái. Mục 5 chuỗi. Mục 6 màu.
     Mục 7 chuyển động.
  2. docs/contracts/billing/ui.md (T3) — props THẬT của mọi component bạn được dùng, cú pháp lớp
     Tailwind cho token, lớp breakpoint responsive.
  3. docs/contracts/billing/format-motion.md (T1) — durationMs và thang thời lượng.
  4. src/screens/onboarding/WelcomeScreen/WelcomeScreen.tsx — khuôn view thuần gần nhất (318 dòng).
  5. LUAT_MAN_HINH.md R-60 (view thuần), R-22 (trần 400 dòng), R-65, R-71, R-72.
KHÔNG viết hook, KHÔNG viết test, KHÔNG viết story. Hợp đồng hoặc notes sai/thiếu →
`orca orchestration ask --question "..." --timeout-ms 600000 --json` rồi DỪNG chờ trả lời.

[VIỆC CỦA BẠN]
Viết src/screens/billing/BillingScreen/BillingScreen.tsx — view thuần, nhận TẤT CẢ qua props,
test được chỉ từ props. Xuất: BillingScreen, BillingScreenProps (và các kiểu con ở hợp đồng mục 2).

R-60 LÀ LUẬT QUAN TRỌNG NHẤT CỦA BẠN: file này KHÔNG được import @/api, @/store, @/domain,
@/lib/http. Luật ESLint local/no-data-layer-in-view sẽ chặn. Được phép: @/components/**,
@/lib/motion, @/lib/format (chỉ nếu thật cần — nhưng KHÔNG cần, vì mọi *Label đã là chuỗi).

BỐ CỤC — nguyên văn từ prompt gốc:
Trên --bg-app, bốn khối dọc cách nhau 24. Dải so sánh gói rộng tối đa 1120; các khối còn lại rộng
960, đều căn giữa.
1. Gói hiện tại — thẻ trắng bo 12 đệm 24: tên gói h2 · dòng dùng "1.842 / 5.000 m² đã số hoá trong
   chu kỳ này" · thanh 6px nền --bg-sunken, phần đầy --accent · ngày gia hạn · nút chìm "Đổi gói".
2. So sánh gói — ba thẻ trắng bo 16 đệm 28 CAO BẰNG NHAU: tên gói h3 · giá chữ đều mono-lg với đơn vị
   --text-secondary bên dưới · nét mảnh · sáu dòng tính năng, mỗi dòng một dấu tích 18 màu
   --text-secondary (KHÔNG màu xanh) · một nút ở đáy. Gói khuyến nghị CHỈ được đánh dấu bằng viền 1px
   --accent (phán quyết Q3: --accent-border không tồn tại) và một badge nhỏ nền --accent-wash.
3. Ước tính — một câu văn xuôi nêu đơn giá thị trường và chi phí tương đương với sản phẩm này, cả hai
   số bằng chữ đều, đặt thẳng trên nền ấm, KHÔNG hộp gọi. Kèm bảng ba dòng.
   Câu này đến từ props.estimate.sentence là mảng 5 mảnh — render mảnh 1 và 3 bằng chữ đều, các mảnh
   còn lại là văn xuôi. TUYỆT ĐỐI KHÔNG nối chuỗi số trong JSX.
4. Hoá đơn — bảng Mã · Kỳ · Diện tích · Số tiền (chữ đều CĂN PHẢI) · Trạng thái · IconButton tải PDF.
   10 dòng một trang.
SegmentedControl đổi kỳ thanh toán đặt TRÊN khối 2.
Dưới 1024: ba thẻ gói xếp dọc, bảng hoá đơn đổi thành thẻ.

BẢY TRẠNG THÁI (hợp đồng mục 4) — view render đủ cả bảy theo props.state. Trạng thái Đang tải là
8 dòng khung xương + ba thẻ gói khung xương. Trạng thái Một phần: dải cảnh báo CHỈ nằm trong khối
của nó (props.degraded[].block). Trạng thái Không có quyền: hiện TOÀN BỘ ở chế độ đọc + câu
props.readOnlyNotice, không ẩn khối nào.

CHUYỂN ĐỘNG (hợp đồng mục 7): thanh hạn mức chạy 0 → props.plan.usedRatio trong durationMs('slow');
thẻ gói nâng -1px khi trỏ chuột trong durationMs('instant'); thẻ khuyến nghị KHÔNG được động khác ba
thẻ kia. Cấm số thời lượng viết tay (R-71). framer-motion chỉ được dùng qua @/components/motion (R-39).

KHẢ NĂNG TIẾP CẬN (A12/R-72): mọi IconButton có aria-label; bảng có cấu trúc heading đúng; thanh hạn
mức có vai trò và nhãn cho trình đọc màn hình; bàn phím đi hết được màn.

Chuỗi tiếng Việt viết thẳng vào TSX, lấy đúng bảng hợp đồng mục 5, chữ THƯỜNG kiểu câu (A6).
Khoá mới → docs/contracts/billing/i18n/t6.fragment.json. KHÔNG tự sửa src/i18n/vi.json.

TRẦN 400 DÒNG (R-22) là cứng. Nếu bạn thấy sắp vượt: DỪNG và
`orca orchestration ask` hỏi điều phối viên, KHÔNG tự tách file (tách file là đổi bảng chủ sở hữu).

[CẤM TUYỆT ĐỐI — nguyên văn từ prompt gốc]
- Không thẻ gói nào có nền màu, không băng nhiều màu, không "phổ biến nhất" nổi bật.
- Dấu tích tính năng màu trung tính, không xanh lá.
- Không đỏ đặc cho hoá đơn quá hạn; dùng badge nền nhạt.
- Không tự định dạng số, không tự tính tạm tính.
- Không tạo component mới.
[KHÔNG ĐƯỢC SỬA FILE NÀO]
- src/lib/**, src/api/**, src/domain/**, src/store/**, src/components/**, AGENTS.md, các màn đã xong.
Thêm: không TODO/FIXME/stub, không `any`, không sửa/tắt bài kiểm có sẵn, không mã màu thô (A1 —
hex/rgb/hsl bị local/no-raw-color chặn), không sửa CONTRACT.md, không sửa file của T4/T5.

[FILE ĐƯỢC SỬA — ĐÚNG HAI]
- src/screens/billing/BillingScreen/BillingScreen.tsx
- docs/contracts/billing/i18n/t6.fragment.json
Không file nào khác.

[NGHIỆM THU — PHẢI IN SỐ VÀO worker_done]
  pnpm typecheck        → 0 lỗi
  pnpm lint             → 0 cảnh báo, 0 lỗi
  pnpm test             → "N passed | M failed", không làm đỏ bài kiểm nào đang xanh
  pnpm length           → SỐ DÒNG FILE CỦA BẠN, phải < 400
  rg -n "#[0-9a-fA-F]{3,8}|rgb\(|hsl\(" src/screens/billing  → phải RỖNG, dán kết quả
  rg -n "from '@/(api|store|domain|lib/http)" src/screens/billing/BillingScreen/BillingScreen.tsx
                        → phải RỖNG, dán kết quả
  git diff --name-only  → đúng 2 dòng
Trong --body ghi thêm: số dòng file, 7 trạng thái đã render (liệt kê), breakpoint đã dùng,
cách đánh dấu thẻ khuyến nghị (một dòng). Thiếu số = chưa xong. Commit trước khi gửi worker_done.
