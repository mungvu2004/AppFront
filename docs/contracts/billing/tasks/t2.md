T2 · contract-data-perms — LỚP 1, khảo sát, KHÔNG viết mã sản phẩm

[BỐI CẢNH — HỢP ĐỒNG VÀ NOTES]
Bạn là worker trong DAG 7 task dựng màn /billing của repo AppFront. Điều phối viên đang giám sát.
Đọc trước: docs/contracts/billing/CONTRACT.md (hợp đồng đông cứng, đã duyệt) và LUAT_MAN_HINH.md
R-64 (trạng thái máy chủ cắm vào src/lib/query, KHÔNG dựng lại) + R-69 (thiếu logic thì DỪNG và hỏi).
Hợp đồng đã chốt: KHÔNG có tầng dữ liệu billing, nên màn dùng gateway trong thư mục màn (nợ T-09).
Việc của bạn là chứng minh điều đó bằng lệnh, và bóc chữ ký của mọi thứ T5 sẽ cắm vào.
Hợp đồng sai → `orca orchestration ask --question "..." --timeout-ms 600000 --json`, DỪNG chờ trả lời.

[VIỆC CỦA BẠN]
Viết docs/contracts/billing/data.md. Mỗi mục: chữ ký đầy đủ + đường dẫn:số dòng + cách gọi đúng.

1. TRẠNG THÁI MÁY CHỦ — src/lib/query/*: queryKeys (cấu trúc factory, createQueryKeyFactory),
   cachePolicy, invalidation, prefetch, queryClient. Ghi rõ: KHÔNG có nhánh billing/invoice/quota
   (dán kết quả `rg -n "billing|invoice|quota" src/lib/query src/api/endpoints.ts`). Chỉ ra cách
   một màn khai khoá query cục bộ mà KHÔNG sửa src/lib — đọc cách các màn đã xong đang làm
   (src/screens/account/AccountSettings/useAccountSettings.ts, src/screens/dashboard/ProjectDashboard/**)
   và ghi lại khuôn đó nguyên văn.
2. MUTATION — src/lib/mutations/*: createOptimisticMutation, undoTicket (UNDO_WINDOW_MS), coalesce,
   entityQueue, flushPolicy, notificationBus. Cái nào hợp cho "nâng gói" (A8 hoàn tác được?) và
   cái nào KHÔNG hợp vì A9 nói hành động không hoàn tác được thì phải hỏi trước bằng hộp thoại.
   Nêu phán quyết của bạn kèm trích dẫn dòng.
3. LỖI (L-03) — src/lib/errors/*: toAppError, describeError, ErrorDescription (từng trường),
   AppError, AppErrorKind, APP_ERROR_KINDS, APP_ERROR_KIND_CONFIG, AppErrorSeverity, AppErrorRecovery,
   reportError. Trả lời chính xác: "mã ngắn" mà prompt yêu cầu hiện ra từ trường nào? Nếu không có
   trường mã → ghi NOT FOUND và đề xuất lấy từ đâu (không tự chế mã lỗi mới).
4. QUYỀN (trạng thái 6) — src/lib/auth/permissions.ts: AUTH_ROLES, PermissionKey, permissionMatrix,
   can(). src/types/project.ts: ProjectRole. src/hooks/useSession.ts + src/lib/auth/session.ts:
   getSession, subscribeToSession, SessionSnapshot (trường roles). Viết ra ĐÚNG biểu thức mà hook
   phải dùng để trả lời "vai này có được đổi gói không". Nếu ma trận quyền không có khoá nào hợp
   cho billing → ghi NOT FOUND và đề xuất phương án KHÔNG sửa src/lib.
5. P-07 THANG MÀU NGƯỠNG (phán quyết Q8 — bạn là người chốt) — đọc src/lib/coloring/scales.ts
   (SEQUENTIAL_RAMP, createQuantileScale, createLookupScale, bandIndexOf, ColorTokenName) và
   src/lib/coloring/legend.ts. Trả lời: hàm/hằng nào là "thang màu nhạt cho ngưỡng" hợp với thanh
   hạn mức 2 mức (bình thường / cần chú ý)? So sánh với nhánh --state-attention-* trong
   COLOR_TOKEN_NAMES. Kết luận PHẢI là một trong hai: (a) tên hàm có thật + cách gọi, hoặc
   (b) NOT FOUND + đề xuất dùng token trạng thái nào và vì sao — kèm trích dẫn dòng. A4 nói đúng
   BA màu trạng thái, đừng đề xuất màu thứ tư.
6. TIỀN LỆ GATEWAY — đọc và tóm tắt src/screens/account/AccountSettings/accountSettingsGateway.ts
   (đặc biệt JSDoc dòng 1-32) và src/screens/project/ProjectSettings/projectSettingsGateway.ts.
   Ghi lại khuôn: hình dạng interface, vì sao trả Promise, cách đặt hàm reset cho test, cách ghi nợ.
   T4 sẽ chép khuôn này.
7. Cách bảy trạng thái được suy ra từ useQuery trong màn đã xong — đọc
   src/screens/onboarding/WelcomeScreen/useWelcomeScreen.ts và ghi lại khuôn ánh xạ
   (isPending/isError/data rỗng/quyền) sang 7 trạng thái. T5 sẽ chép khuôn này.

Cuối file: mục "NOT FOUND" liệt kê mọi thứ hợp đồng nhắc mà mã không có.

[CẤM TUYỆT ĐỐI — nguyên văn từ prompt gốc]
- Không tự định dạng số, không tự tính tạm tính.
- Không tạo component mới.
[KHÔNG ĐƯỢC SỬA FILE NÀO]
- src/lib/**, src/api/**, src/domain/**, src/store/**, src/components/**, AGENTS.md, các màn đã xong.
Thêm: không TODO/FIXME/stub, không sửa hay tắt bài kiểm có sẵn, không sửa CONTRACT.md.

[FILE ĐƯỢC SỬA — CHỈ MỘT]
- docs/contracts/billing/data.md
Không file nào khác. Không tạo file trong src/.

[NGHIỆM THU — PHẢI IN SỐ VÀO worker_done]
  pnpm typecheck        → 0 lỗi
  git diff --name-only  → đúng 1 dòng
Trong --body ghi: số mục đã phủ (n/7), số dòng notes, PHÁN QUYẾT Q8 một dòng, danh sách NOT FOUND.
Thiếu số = chưa xong. Commit bằng git trước khi gửi worker_done.
