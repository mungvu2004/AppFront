T7 · integrate + acceptance — LỚP 3, task cuối: ráp, đăng ký route, bộ kiểm, nghiệm thu định lượng

[BỐI CẢNH — HỢP ĐỒNG VÀ NOTES]
Bạn là worker CUỐI trong DAG 7 task dựng màn /billing của repo AppFront. Điều phối viên đang giám sát
và sẽ dựng một decision gate với người dùng dựa trên SỐ bạn in ra. Sáu task trước đã xong, kết quả
nằm trên nhánh bạn đang đứng. Đọc BẮT BUỘC:
  1. docs/contracts/billing/CONTRACT.md — hợp đồng đông cứng đã duyệt. Mục 1 bảng chủ sở hữu,
     mục 3.1 năm chuỗi nghiệm thu, mục 4 bảy trạng thái, mục 9 cổng nghiệm thu (đọc kỹ cổng DELTA).
  2. docs/contracts/billing/{format-motion,data,ui}.md — notes của T1, T2, T3.
  3. src/screens/billing/BillingScreen/{billingGateway.ts,useBillingScreen.ts,BillingScreen.tsx}
  4. src/screens/onboarding/WelcomeScreen/ — TOÀN BỘ 6 file. Đây là khuôn gần nhất, chép nó:
     index.ts (có bảng chủ sở hữu trong JSDoc), .container.tsx (ScreenErrorBoundary + XxxRoute),
     .stories.tsx (7 story), .test.tsx (expectSevenStates + expectVietnamese + expectAccessible).
  5. LUAT_MAN_HINH.md — Phần 4 (khối lệnh kiểm) và Phần 5 (danh sách tự kiểm trước khi mở PR).
Hợp đồng hoặc mã của người khác sai → `orca orchestration ask --question "..." --timeout-ms 600000 --json`
rồi DỪNG chờ trả lời. ĐẶC BIỆT: nếu BillingScreenViewModel (T5) và BillingScreenProps (T6) lệch nhau
ở bất kỳ trường nào, ĐỪNG tự sửa file của họ — hỏi điều phối viên.

[VIỆC CỦA BẠN]
1. src/screens/billing/BillingScreen/index.ts — đường nhập duy nhất. JSDoc tiếng Việt kèm BẢNG CHỦ
   SỞ HỮU (chép khuôn WelcomeScreen/index.ts). Gộp kiểu: BillingScreenViewModel và BillingScreenProps
   là MỘT kiểu — nối bằng `import type`, không chép lần thứ ba.
2. src/screens/billing/BillingScreen/BillingScreen.container.tsx — bọc ScreenErrorBoundary của
   @/components/feedback (R-62; KHÔNG dùng bản ở src/lib/screen-state), phần dự phòng dựng bằng
   EmptyState từ report.description. Xuất BillingScreenContainer và BillingRoute.
   R-73: container phải nhận đủ props để màn khác mở được ngay, kể cả khi chưa có nơi gọi thật.
3. src/routes/router.tsx — thay `<Placeholder name="/billing" />` bằng route thật, lazy, bọc
   `suspended(...)` đúng khuôn các route đã có. Sau đó `rg -c "Placeholder" src/routes/router.tsx`
   phải GIẢM ĐÚNG MỘT so với trước (R-66). KHÔNG sửa src/routes/paths.ts (Q1: '/billing' giữ nguyên).
4. src/i18n/vi.json — gộp MỌI docs/contracts/billing/i18n/*.fragment.json vào đúng một nhánh
   "billing". Giữ nguyên phần còn lại của file, không sắp xếp lại, không xoá khoá của ai.
5. src/screens/billing/BillingScreen/BillingScreen.stories.tsx — ĐỦ BẢY story, một story một trạng
   thái (R-63).
6. src/screens/billing/BillingScreen/BillingScreen.test.tsx — bộ kiểm. Bắt buộc có:
   a. expectSevenStates — phải in "expectSevenStates = 7/7" kèm danh sách (chép khuôn
      WelcomeScreen.test.tsx dòng ~277-284).
   b. expectVietnamese + expectAccessible trên cây render của trạng thái đầy đủ.
   c. expectNoRawColor.
   d. NĂM CHUỖI ĐỊNH DẠNG — một bài kiểm in ra và khẳng định đúng năm giá trị ở hợp đồng mục 3.1:
      formatArea(2480)="2.480,00 m²" · formatMoney(1240000)="1.240.000 ₫" ·
      formatArea(620,{fractionDigits:0})="620 m²" · formatArea(1842,{fractionDigits:0})="1.842 m²" ·
      formatMoney(200000)="200.000 ₫". Dùng console.log để số hiện ra trong log chạy kiểm.
   e. HẠN MỨC 85% — ép gateway sang cảnh 4380/5000, khẳng định tone đổi sang thang cần chú ý VÀ
      InlineAlert "Sắp hết hạn mức. Còn 620 m²." hiện ra.
   f. ĐỔI KỲ THANH TOÁN — dùng fakeClock, khẳng định priceLabel đi qua ít nhất một giá trị TRUNG GIAN
      giữa giá cũ và giá mới (chứng minh CHẠY SỐ, không NHẢY). Không được chỉ khẳng định giá trị cuối.
   g. VAI KỸ SƯ và NGƯỜI XEM — khẳng định chế độ đọc và câu "Chỉ quản trị viên có thể thay đổi gói."
   h. NÂNG GÓI — khẳng định bảng tóm tắt xác nhận (có số tiền chia theo tỷ lệ) hiện ra TRƯỚC khi chốt.
   R-70: KHÔNG sửa/nới/tắt bài kiểm có sẵn của repo, không .skip, không .only.
7. XOÁ SẠCH docs/contracts/billing/** (cả CONTRACT.md, notes, fragment) trong commit CUỐI, để
   `git diff --name-only master...HEAD` chỉ còn: src/screens/billing/**, src/routes/router.tsx,
   src/i18n/vi.json. Đây là yêu cầu R-68.

[CẤM TUYỆT ĐỐI — nguyên văn từ prompt gốc]
- Không thẻ gói nào có nền màu, không băng nhiều màu, không "phổ biến nhất" nổi bật.
- Dấu tích tính năng màu trung tính, không xanh lá.
- Không đỏ đặc cho hoá đơn quá hạn; dùng badge nền nhạt.
- Không tự định dạng số, không tự tính tạm tính.
- Không tạo component mới.
[KHÔNG ĐƯỢC SỬA FILE NÀO]
- src/lib/**, src/api/**, src/domain/**, src/store/**, src/components/**, AGENTS.md, các màn đã xong.
Thêm: không TODO/FIXME/stub, không `any`, không sửa file nguồn của T4/T5/T6 (billingGateway.ts,
useBillingScreen.ts, BillingScreen.tsx) — lệch hợp đồng thì HỎI, không tự sửa. Không sửa
src/routes/paths.ts. Không nới ngân sách kích thước gói.

[FILE ĐƯỢC SỬA]
- src/screens/billing/BillingScreen/index.ts
- src/screens/billing/BillingScreen/BillingScreen.container.tsx
- src/screens/billing/BillingScreen/BillingScreen.stories.tsx
- src/screens/billing/BillingScreen/BillingScreen.test.tsx
- src/routes/router.tsx
- src/i18n/vi.json
- xoá docs/contracts/billing/** (commit cuối)
Không file nào khác.

[NGHIỆM THU — BẢNG SỐ SONG SONG, DÁN NGUYÊN VĂN VÀO worker_done]
Chạy trọn khối lệnh LUAT_MAN_HINH.md Phần 4 với SCREEN=src/screens/billing/BillingScreen và dán
nguyên văn kết quả từng dòng. Rồi:
  pnpm typecheck   → 0 lỗi
  pnpm lint        → 0 cảnh báo, 0 lỗi
  pnpm test        → "N passed | M failed"
  pnpm coverage    → ngưỡng src/domain 90% / src/lib 80%
  pnpm length      → mọi file màn < 400
  pnpm build && pnpm size → CỔNG DELTA: đạt khi tổng JS <= 431,8 KiB VÀ chunk lớn nhất vẫn 132,9 KiB.
     Mốc trên master sạch: tổng JS 419,8 KiB / ngân sách 175 KiB / chunk lớn nhất 132,9 KiB.
     Báo cáo PHẢI ghi "kích thước gói: ĐỎ TỪ TRƯỚC (419,8/175)" — TUYỆT ĐỐI KHÔNG ghi "đạt" (E.10/R-58).
  rg -c "Placeholder" src/routes/router.tsx  → dán số trước và sau
  git diff --name-only master...HEAD         → dán TOÀN BỘ, phải chỉ có ba nhóm cho phép
Trong --body: bảng số song song (bước | mốc master | sau khi ráp), expectSevenStates n/7, năm chuỗi
định dạng in ra nguyên văn, kết quả cảnh 85%, kết quả kiểm chạy số (giá trị trung gian quan sát được),
và danh sách việc còn nợ. Bước nào KHÔNG chạy thì ghi "chưa chạy", cấm ghi "đạt" (E.10).
Thiếu số = chưa xong. Commit trước khi gửi worker_done.
