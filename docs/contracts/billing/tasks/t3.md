T3 · contract-ui-testing — LỚP 1, khảo sát, KHÔNG viết mã sản phẩm

[BỐI CẢNH — HỢP ĐỒNG VÀ NOTES]
Bạn là worker trong DAG 7 task dựng màn /billing của repo AppFront. Điều phối viên đang giám sát.
Đọc trước: docs/contracts/billing/CONTRACT.md (đã duyệt), mục 2 (kiểu view), mục 4 (bảy trạng thái),
mục 6 (màu và badge). CLAUDE.md bất biến A1 (màu từ token), A6 (nhãn tiếng Việt thường, kiểu câu),
A11 (bảy trạng thái), A12 (bàn phím hạng nhất, Esc đóng lớp trên cùng).
T6 sẽ dựng view CHỈ từ notes của bạn — props sai một chữ là T6 không biên dịch được.
Hợp đồng sai → `orca orchestration ask --question "..." --timeout-ms 600000 --json`, DỪNG chờ.

[VIỆC CỦA BẠN]
Viết docs/contracts/billing/ui.md. Mỗi component: interface props ĐẦY ĐỦ chép nguyên văn từ mã,
đường dẫn:số dòng, đường import đúng, và một ví dụ JSX tối thiểu.

1. src/components/ui/Table.tsx — TOÀN BỘ họ: Table root và mọi thành phần con
   (Header/Body/Row/HeaderCell/Cell/Empty/Skeleton hay tên thật là gì). Ghi rõ cách gộp
   (Object.assign?), cách sắp xếp (sortKey/sortDir/onSort), cách căn phải một cột số, và thành
   phần nào dựng khung xương cho 8 dòng.
2. src/components/ui/Badge.tsx — BadgeVariant đủ 4 giá trị, prop noDot, lớp nền của từng variant.
3. src/components/ui/Button.tsx + buttonVariants.ts — mọi variant và size. Chỉ ra cái nào là
   "nút chìm" (ghost/subtle) mà prompt yêu cầu cho nút "Đổi gói".
4. src/components/ui/SegmentedControl.tsx — SegmentedControlProps, SegmentedControlOption,
   useSegmentedControl, SegmentedItem, SegmentedRoot. Cách dùng có điều khiển (value + onChange).
5. src/components/ui/IconButton.tsx — props, và yêu cầu aria-label cho nút chỉ có biểu tượng (A12).
   Kèm: biểu tượng lấy từ đâu trong repo (lucide-react?) — dán một dòng import có thật đang dùng.
6. src/components/ui/FieldRow.tsx — props, dùng cho ba dòng bảng "Ước tính" có hợp không (nêu ý kiến).
7. src/components/feedback/InlineAlert.tsx — InlineAlertProps, InlineAlertLevel, hình dạng prop
   action. Đây là thứ dựng cảnh báo hạn mức và trạng thái lỗi.
8. src/components/feedback/Skeleton.tsx — SkeletonPreset đủ giá trị. Preset nào cho 8 dòng bảng,
   preset nào cho ba thẻ gói.
9. src/components/feedback/EmptyState.tsx — props, dùng cho trạng thái Rỗng.
10. src/components/feedback/ScreenErrorBoundary.tsx — chữ ký export, props, hình dạng report
    (report.description), và cách src/App.tsx đang gắn nó (key={...}). T7 sẽ chép khuôn này.
11. src/components/feedback/Toast.tsx — Toast, useToast: chữ ký, cách một màn đã xong dùng nó.
12. BỘ KIỂM — chữ ký đầy đủ của: src/lib/testing/expectSevenStates.ts (ScreenRenderer,
    ScreenRenderResult, hàm ném lỗi thế nào), sevenStateScenarios.ts (SEVEN_STATES,
    SEVEN_STATE_LABELS, SevenStateScenario, SevenStateRow, SevenStateScenarioOptions,
    createSevenStateScenarios), render.tsx (renderWithProviders — nhận gì), expectVietnamese.ts
    (tuỳ chọn ignore/allowWords), expectAccessible.ts, expectNoRawColor.ts.
13. TOKEN MÀU CÓ THẬT — dán COLOR_TOKEN_NAMES (src/lib/coloring/scales.ts:62-135) và đối chiếu với
    src/styles/globals.css. Xác nhận có: --bg-app, --bg-surface, --bg-sunken, --accent,
    --accent-wash, --text-secondary, --border-default, --state-attention-*, --state-violation-*.
    Xác nhận NOT FOUND: --accent-border (bằng chứng của phán quyết Q3).
    Kèm: cách viết một token thành lớp Tailwind trong repo này (đọc tailwind.config.ts, dán ví dụ
    có thật từ một component đã xong) — T6 cần biết viết bg-bg-sunken hay bg-[--bg-sunken].
14. Cách một màn đã xong làm responsive dưới 1024 (đọc ProjectDashboard.tsx hoặc AccountSettings.tsx),
    dán lớp breakpoint có thật.

Cuối file: mục "NOT FOUND".

[CẤM TUYỆT ĐỐI — nguyên văn từ prompt gốc]
- Không thẻ gói nào có nền màu, không băng nhiều màu, không "phổ biến nhất" nổi bật.
- Dấu tích tính năng màu trung tính, không xanh lá.
- Không đỏ đặc cho hoá đơn quá hạn; dùng badge nền nhạt.
- Không tự định dạng số, không tự tính tạm tính.
- Không tạo component mới.
[KHÔNG ĐƯỢC SỬA FILE NÀO]
- src/lib/**, src/api/**, src/domain/**, src/store/**, src/components/**, AGENTS.md, các màn đã xong.
Thêm: không TODO/FIXME/stub, không sửa hay tắt bài kiểm có sẵn, không sửa CONTRACT.md.

[FILE ĐƯỢC SỬA — CHỈ MỘT]
- docs/contracts/billing/ui.md
Không file nào khác. Không tạo file trong src/.

[NGHIỆM THU — PHẢI IN SỐ VÀO worker_done]
  pnpm typecheck        → 0 lỗi
  git diff --name-only  → đúng 1 dòng
Trong --body ghi: số mục đã phủ (n/14), số dòng notes, xác nhận --accent-border NOT FOUND, và
cú pháp lớp Tailwind cho token (một dòng). Thiếu số = chưa xong. Commit trước khi gửi worker_done.
